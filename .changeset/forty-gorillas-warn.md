---
'@openai/chatkit': minor
---

- Add `border` prop to Card widgets.
- Add types for `Table`, `Table.Row`, and `Table.Cell` widget components.
- Allow BasicRoot widgets in ChatKit threads and update types to reflect that support.
- Add the `share` and `bug` icons to the `HeaderIcon` union.
- Add `thread.autoScroll` option.
- Allow `composer.attachments.maxSize` option to take per-mimetype limits.
- Allow `setComposerValue` method to unset selected tool id with `null`.
